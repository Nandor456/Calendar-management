# In-memory profile: no database configuration needed.
# This profile activates repositories annotated with @Profile("in-memory").

# You can optionally set a server port for local testing:
#server.port=8081

