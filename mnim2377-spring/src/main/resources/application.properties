# Default settings shared across all profiles go here.
#
# Don't hard-force a profile here. Select one at runtime:
#   --args='--spring.profiles.active=in-memory'
#   --args='--spring.profiles.active=jdbc'
#   --args='--spring.profiles.active=jpa'
#
#spring.profiles.active=jpa

